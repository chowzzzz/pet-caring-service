<!DOCTYPE html>
<html lang="en">

<head>
	<link rel="stylesheet" href="/stylesheets/profile.css" />
	<%- include('./partials/head'); %>
</head>

<body>
	<%- include('./partials/navbar'); %>
	<%- include('./partials/caretaker-navbar'); %>
	<main>
		<div class="jumbotron">
			<h1 class="display-1">Caretaker Availablity Dates</h1>
			<h1 class="display-4"><%= details[0].username %></h1>
			<br />
			<% if (!isFulltime) { %>
			<table class="table">
				<thead class="thead-dark">
					<tr>
						<th scope="col">Start Date</th>
						<th scope="col">End Date</th>
					</tr>
				</thead>
				<% for (var i = 0; i < available.length; i++) { %>
				<tr>
					<td scope="row" data-title="Start Date"><%= available[i].startdate %></td>
					</td>
					<td scope="row" data-title="End Date"><%= available[i].enddate %></td>
					</td>
				</tr>
				<% } %>
			</table>
			<% } else { %>
			<h1> Only part-time caretakers can indicate availabilities!</h1>
			<% }%>
		</div>
	</main>
</body>

</html>